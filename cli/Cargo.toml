[package]
name = "git-b-cli"
version.workspace = true
edition.workspace = true
description = "CLI definitions for git-b"
publish = false

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
